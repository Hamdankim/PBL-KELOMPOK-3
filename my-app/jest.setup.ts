// Setup file untuk Jest
// Ini dijalankan sebelum semua test files

import '@testing-library/jest-dom'

// Optional: Setup global mocks, jika diperlukan
// Contoh: mock firebase, mock next router, dll
